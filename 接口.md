| 接口           | 方向        | 核心作用        | 主要传输数据                                |
| ------------ | --------- | ----------- | ------------------------------------- |
| **视觉目标检测接口** | 视觉 → UI   | UI显示识别结果    | 目标ID、类别/靶标ID、粗略位置、置信度、是否识别            |
| **视觉位姿接口**   | 视觉 → 算法控制 | 给控制算法目标位置   | 目标6DoF位姿 `x,y,z,qx,qy,qz,qw`、时间戳、有效标志 |
| **任务指令接口**   | UI → 算法控制 | 用户下达机器人任务   | 目标ID、任务类型、开始/停止/抓取等指令                 |
| **控制状态接口**   | 算法控制 → UI | UI显示机器人运行状态 | 当前任务、运动状态、是否到达、算法状态、错误信息              |
| **视觉状态接口**   | 视觉 → UI   | UI显示相机/识别状态 | 相机连接状态、目标丢失、当前距离、视觉误差等                |


# 任务安排



| 时间        | 主要任务                 | 具体工作                                                                    | 预期成果                                                        |
| --------- | -------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------- |
| **周三**    | **完成视觉/UI与控制算法接口联调** | 与控制部分统一数据格式和通信方式；打通目标位姿、控制命令和机器人状态的双向传输                  | 成功集成项目，连接相机，视觉能够向控制算法发送目标6DoF位姿和命令；控制算法能够向UI返回机器人状态和末端位置 |
| **周四**    | **完善UI展示与控制功能**      | 根据联调后的接口修改UI；增加蛇形机器人末端/夹爪实时位置显示；增加命令发布按钮及执行状态显示；处理目标丢失、通信异常等状态 | UI能够实时显示视觉目标信息、末端位置和系统状态，并能够发布目标选择、移动、抓取、停止等命令              |
| **周五**    | **开始相机精度调试与测试**      | 优化D405标定；测试不同距离、角度下编码靶标的定位效果；对比视觉测量值与真实值；分析位置和姿态误差来源                    | 得到第一版相机精度测试结果和误差表，为后续提高6DoF位姿精度提供依据                         |
| **后续** | **视觉精度优化与闭环联调**      | 根据周五测试结果优化圆点检测、深度取值、PnP/位姿计算和滤波；将优化后的视觉数据重新接入控制链路                       | 完成较稳定的“视觉识别 → 位姿输出 → 运动控制 → 状态反馈”闭环                         |
